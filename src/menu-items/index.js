import dashboard from './dashboard';
import pages from './pages';
import GestionVentas from './ventas';
import RRHH from './rrhh';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [dashboard, pages, GestionVentas, RRHH]
};

export default menuItems;

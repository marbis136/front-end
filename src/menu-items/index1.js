import dashboard from './dashboard';
import GestionVentas from './ventas';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [dashboard, GestionVentas]
};

export default menuItems;
